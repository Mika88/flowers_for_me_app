<h2>Your Orders</h2>
<% @orders.each_with_index do |order, i| %>
    <p><%= i + 1%>.<%= link_to "#{order.arrangement.title}", user_order_path(order.user_id, order) %> - <%= order.arrangement.price %></p>
<% end %>    

